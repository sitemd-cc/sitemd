---
# Data provider: none, supabase, firebase, airtable, rest
provider: none

# Cache duration in seconds (0 = no cache)
cacheTTL: 300

# Loading text shown while fetching
loadingText: Loading...

# Empty state text when no results
emptyText: No items found.

# Error text on fetch failure
errorText: Unable to load data.

# ── Provider settings ──
# supabaseUrl:
# firebaseProjectId:
# airtableBaseId:
# restBaseUrl:

# ── Secrets (CLI-managed) ─────────────────────────
# Manage secrets: sitemd config setup data
# (none configured)
---
