---
# Deploy Settings

# Deploy target: cloudflare, github, netlify, vercel
# target: cloudflare

# ── Cloudflare Pages ──
# cloudflareProject:
# cloudflareAccountId:
# cloudflareBranch:

# ── Netlify ──
# netlifySiteId:

# ── Vercel ──
# vercelProjectId:
# vercelTeamId:

# ── GitHub Pages ──
# githubRepo:
# githubBranch: gh-pages

# Media hosting: false, r2, s3
# mediaHosting: false

# CDN URL prefix for media files
# mediaCdnUrl:

# ── R2 media hosting ──
# r2AccountId:
# r2Bucket:

# ── S3 media hosting ──
# s3Region: us-east-1
# s3Bucket:

# Only deploy pages matching these groups or paths (empty = all)
# deployPages:
#   - blog
#   - docs

# ── Secrets (CLI-managed) ─────────────────────────
# Manage secrets: sitemd config setup deploy
# (none configured)
---
